import os
try:
    Import('env')
except:
    env = Environment()
    Export('env')
    
limaObjects = []



SConscript([os.path.join('..','common','SConstruct'),
            os.path.join('..','hardware','SConstruct'),
            os.path.join('..','control','SConstruct'),
            os.path.join('..','camera','SConstruct')])


env.SharedLibrary('liblima',[os.path.join('..','third-party','Sps','Src','sps.c')],
                  LIBS=['libCommon',
                        'libHardware',
                        'libControl',
                        'libCamera',
                        'libprocesslib'],
                  LIBPATH=[os.path.join('..','common','src'),
                           os.path.join('..','camera','src'),
                           os.path.join('..','control','src'),
                           os.path.join('..','hardware','src')
                           os.path.join('..','third-party','Processlib','build'))
